# ~/.bashrc-localhost
# shellcheck disable=SC2148,SC1090,SC2034

# localhost setup
export MAKEFLAGS=j9

find "$HOME/.bashrc-companies/" -type f | while read -r companyFile; do
	source "$companyFile"
done
